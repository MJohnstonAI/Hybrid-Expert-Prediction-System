# PowerBall XTRA — Prospective PB Pattern Hypothesis

**Target:** Friday 2026-08-14  
**Mode:** `paper_trading_only`  
**Timestamp:** 2026-08-14 08:59 SAST  
**Dataset:** South African PowerBall XTRA only, 2026-06-02 through 2026-08-11

## Human pattern hypothesis

Project director identifies the strict PowerBall regime motif:

`TEENS (>10) -> LOW (<10) -> LOW (<10) -> TEENS (>10)`

The current suffix is:

`13 -> 6 -> 2 -> ?`

Therefore the prospective directional hypothesis for the 2026-08-14 XTRA PowerBall is:

`PB > 10`, i.e. candidate set `{11,12,13,14,15,16}`.

## Historical motif check

Before the current unresolved suffix, the exact strict three-state antecedent `>10, <10, <10` occurred twice in the June-August 2026 XTRA ledger:

1. `11 -> 1 -> 1 -> 15`
2. `15 -> 4 -> 8 -> 15`

Both completed occurrences resolved to a PowerBall above 10, and both resolved specifically to **15**.

Current unresolved occurrence:

3. `13 -> 6 -> 2 -> ?`

This creates a prospective exact-ball challenger hypothesis of **PB15**.

## Red-team calibration

- The motif has only two completed historical examples, so this is `INSUFFICIENT_EVIDENCE`, not a validated transition law.
- Under a uniform 1-16 structural null, `P(PB > 10) = 6/16 = 37.5%`.
- The exact PB15 hypothesis has structural-null probability `1/16 = 6.25%`.
- PB10 is deliberately excluded from both classes because the human rule is defined strictly as `>10` versus `<10`.
- The observed PB10 on 2026-07-24 therefore acts as a neutral/boundary state rather than being retrofitted into either side of the pattern.

## Synthesis with the PB2 transition hypothesis

The current PB is 2. The two prior PB2 observations in the active XTRA ledger were followed by PB12 and PB16.

Thus two independent descriptive motifs converge on the upper register:

- strict regime motif `>10,<10,<10 -> >10`: exact historical resolutions `{15,15}`;
- exact predecessor PB2 transition: historical resolutions `{12,16}`.

Prospective protected exact-ball set for tonight:

`{12,15,16}`

with **PB15** promoted to the primary pattern challenger and PB12/PB16 retained as transition hedges.

This file records a pre-draw hypothesis only. No post-draw reinterpretation or parameter change is permitted without a separate audit.
