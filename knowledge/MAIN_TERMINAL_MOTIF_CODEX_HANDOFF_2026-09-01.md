# HEPS Main Terminal-Motif Handoff — HISTORICAL / SUPERSEDED INTERPRETATION

**Original date:** 2026-09-01  
**Updated interpretation:** 2026-09-02  
**Status:** historical hypothesis-generation handoff only

> **WARNING FOR AI AGENTS:** The original version of this handoff proposed investigating a synergy chain of `terminal motif -> HLR -> VVD -> exact coordinate`. That research was executed as E0020. The multiplicative terminal×HLR×VVD coordinate-acquisition chain is now **REJECTED for forward reuse** because the variables are deterministic views of the same sorted-slot transition and the M0->M3 proper scores deteriorated monotonically. Do not reconstruct or reuse that chain as current HEPS advice.

Current binding interpretation:

- `governance/current_method_doctrine.md`
- `governance/methodology_deprecations.md`
- `experiments/E0020/decision.md`
- `core/feature_dictionary.yaml`

## What remains valid from the original handoff

The following research questions remain legitimate **diagnostics**:

- same-slot terminal suffix replay;
- cross-slot motif migration;
- ABA / pair-flip diagnostics;
- alternating two-cycles;
- run-length motifs;
- finite preregistered algebraic rule libraries;
- exact slot-specific terminal nulls;
- marginal-preserving temporal shuffle controls;
- multiplicity correction;
- Main/XTRA state separation.

Terminal research must remain prospectively frozen, multiplicity-aware, and compared with exact slot-specific terminal nulls.

## What was learned in E0020

Discovery replay found:

- a nominal excess of repeated same-slot length-3 words, but not after Holm correction;
- same-slot/cross-slot suffix models did not beat the exact terminal null overall;
- algebraic rules did not establish an edge;
- coordinate proper score worsened when terminal, HLR and VVD residual ratios were multiplied sequentially.

Therefore terminal motifs have **no candidate-acquisition authority**.

## 2026-09-01 prospective target

Actual Main:

`14,16,31,34,40 | PB4`

The frozen terminal shadow did contain useful target-specific information:

- S3 terminal `1` top-1 hit;
- S4 terminal `4` top-1 hit;
- S1 algebraic residue `4` diagnostic hit;
- S2 terminal `6` and S5 terminal `0` appeared in secondary support.

These hits do not override the negative proper-score history. They justify continued **diagnostic/hedge observation only**, not promotion.

## Correct forward research path

If terminal information is revisited, it may be:

1. scored as a separate calibrated terminal probability model;
2. used as a basis function inside one coherent signed-transition model;
3. used for bounded fixed-K preservation when it agrees with genuinely independent evidence.

It may **not** be multiplied with HLR and VVD as independent likelihood ratios.

The current corrected Main acquisition successor is:

`experiments/E0021/`

which uses `MAIN_SIGNED_SLOT_TRANSITION`, exact legal-line normalization, proper-score-first gating, and a separate adjacent-slot preservation challenger.

## Historical provenance

The full original handoff remains available in Git history. It is intentionally not repeated here to reduce future token consumption and prevent obsolete instructions from poisoning new agent context.