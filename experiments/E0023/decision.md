# E0023 Decision

## Decision

`ACCEPT DIAGNOSTIC PROSPECTIVE SHADOW ONLY`

Evidence classification: `INSUFFICIENT_EVIDENCE`.

The null-residualized `Slot1 -> decade shell` conditional score is mathematically coherent and cheap, but the discovery replay does not establish predictive lift.

## Frozen shadow

`MAIN_SLOT1_DECADE_SHELL_ECHO`

For minimum `m` and decade shell `d`:

`P_hat(d|m)=(c(m,d)+20*P0(d|m))/(c(m)+20)`

`R=log(P_hat(d|m)/P0(d|m))`

`P0(d|m)` is exact uniform 5-of-50 combinatorial geometry.

## Discovery evidence

On 19 walk-forward oracle-K13 Main targets with 30 decoy replicates each:

- primary conditional score mean winner percentile: `0.5314`
- 11/19 target means above 0.5
- one-sided sign p: `0.324`
- one-sided t-test p: `0.307`

This is insufficient for predictive promotion.

## Authority

Allowed:

- score all 1,287 completed lines after an upstream K13 is frozen;
- report winner rank/percentile prospectively;
- report the state `(Slot1, decade shell)` and exact-null residual.

Forbidden:

- candidate acquisition;
- changing K;
- hard pruning or decade quotas;
- changing line budget;
- adding the score to `MAIN_ASSEMBLY_DISSENT_OR`;
- claiming repeated Slot1 or decade patterns as predictive by themselves.

## Prospective freeze

First eligible Main target: `2026-09-04`.

Promotion requires repeated prospective incremental winner-rank value beyond E0013, frequency and recency at identical K13/exposure, with tie-safe scoring and no outcome-conditioned retuning.
