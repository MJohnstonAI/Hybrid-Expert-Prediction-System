# E0014 Decision

## Overall decision

`ACCEPT METHODOLOGY TRANSFER / HOLD MAIN-NUMBER PREDICTIVE AUTHORITY`

Overall evidence classification: `INSUFFICIENT_EVIDENCE` for an XTRA main-number breakthrough.

Paper trading only.

## Component dispositions

### Distribution-first structural-residual methodology

`INSUFFICIENT_EVIDENCE` as predictive edge, but **accepted as research discipline**.

Continue full HLR/VVD distributions, exact structural comparators, anywhere-coordinate versus exact-slot separation, and explicit acquisition-versus-assembly attribution.

### Spectral graph — candidate acquisition

`INSUFFICIENT_EVIDENCE`.

No K13 coordinate authority. PPMI degree and spectral row norm are near matched-random exposure and produce no 4/5 or 5/5 target.

### PPMI spectral graph — coalition ranking

`REJECT` the narrow transfer claim that the exact Main E0013 positive-PMI spectral coalition formulation currently reproduces as a positive XTRA signal.

The XTRA replay gives spectral mean winner percentile below 0.5 in unrestricted, morphology-matched and oracle-K13 tests. Do not create a promoted XTRA spectral ranker from this result. A pre-draw diagnostic shadow may be preserved for falsification tracking only, with zero authority.

### Raw pair-count coalition score

`INSUFFICIENT_EVIDENCE`.

It outperforms spectral coherence in the current XTRA replay, but only 10/16 targets are above random median and it has not passed a distinct preregistered permutation/redundancy test. Retain as a research lead, not a new expert.

### VVD residual distributions

`INSUFFICIENT_EVIDENCE`.

No learned distribution beats `NULL_VVD_STRUCTURAL` on both log loss and Brier. Continue distributional scoring; do not promote point or empirical residual authority.

### First-order total-VVD magnitude regime

`REJECT` this specific q25/q75 Markov predictive router. It is worse than exact structural regime probabilities across all tested shrinkages.

### Residual PCA/KMeans router

`REJECT` the tested predictive formulation. Both k=2 and k=3 worsen next total-VVD and next-sum MAE.

### Dynamic sum / parity / decade / gap morphology

`INSUFFICIENT_EVIDENCE` for prediction; retain soft morphology/diversity only. No hard pruning.

### Fixed-K Core + Rescue

`INSUFFICIENT_EVIDENCE`.

One recency or stiction/shadow rescue seat improves the frequency core from 19 to 21 total winner coordinates at fixed K13, but 21 is essentially the random-exposure expectation 20.8. Two rescue seats deteriorate. Continue only as shadow candidate-preservation research.

### Conditional XTRA PowerBall convergence

`PROVISIONAL_SIGNAL`.

Freeze the tau=4 geometric pool of shrinkage-weighted exact-state successor and VVD+HLR successor distributions as a new **prospective shadow derivative**. Historical championship performance receives discovery credit only. No change to the already-frozen 2026-08-28 XTRA prediction.

## Architecture action

- Do not modify `core/heps_architecture.md`.
- Do not copy any Main fitted state into XTRA.
- Do not alter `cycles/2026-08-28/powerball_xtra_pre_draw.md`.
- Preserve E0010 and XTRA E0011 candidate-preservation/regime architecture as experimental.
- Create a separate derivative package for the PowerBall convergence survivor.

## Current bottleneck

The XTRA main-number bottleneck remains **candidate acquisition**. Across the championship, no tested K13 coordinate method demonstrates 4/5 or 5/5 acquisition, and none materially exceeds matched random expectation.

The immediate research target remains: improve prospectively frozen initial winner-coordinate survival at fixed, explicit exposure before giving additional authority to coalition assembly.
