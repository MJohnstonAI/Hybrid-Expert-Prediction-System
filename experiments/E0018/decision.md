# E0018 Decision

## Decision

**ACCEPT METHODOLOGY / AUTHORIZE PROSPECTIVE SHADOW**

Evidence classification remains `INSUFFICIENT_EVIDENCE`.

## Authorized

1. Use `XTRA_HLR_FULL_MIXTURE_BASE` to produce a full-support slot/global shadow field.
2. Feed that field into the frozen E0016 Richardson operator as a separate shadow comparison.
3. Score base, Richardson and fixed blend separately at matched K13/K20 exposure.
4. Use E0011 power-budget, redundancy and proper-score doctrine when interpreting results.
5. Use conservative convergence with no expert-vote multiplier until frozen residual-dependence evidence exists.
6. Preserve positive mass/full-support philosophy before K compression.

## 2026-09-02 dependency clarification

E0018 Stage 1 is **not identical to the rejected E0019 HLR×VVD within-slot product**: Stage 1 uses HLR residual information only.

However, its geometric pooling across sorted slots is still a low-dimensional approximation over mechanically dependent order-statistic slots. Under the current `joint distribution first` doctrine, future successors should compare Stage-1 pooling with coherent legal-line normalization where feasible.

Do not add VVD or terminal residual products to Stage 1 merely to create extra convergence; those are views of the same transition family.

## Richardson clarification

The E0016 pair-separation estimator remains a shadow component, but its historical geometric-mean message-passing update is a heuristic approximation, not exact joint inference. Future E0018/Richardson successors should compare heuristic marginals with direct legal-line pair-potential scoring when feasible.

## Data-provenance gate

The historical `working_extension` path may be used only as explicitly provenance-qualified working-state evidence.

Rows that are `pending_official_source_verification` receive **no canonical confirmatory credit** until verified and moved into the canonical ledger under the normal data rules.

Results must distinguish:

- canonical-ledger scoring;
- working-extension scoring.

Do not merge these evidence classes in a headline result.

## Not authorized

- no hard HLR direction gate;
- no HLR×VVD×terminal multiplication as independent evidence;
- no candidate-union expansion credited as lift;
- no new spectral/co-occurrence candidate acquisition family without a materially new information source;
- no morphology hard-pruning before candidate acquisition scoring;
- no machine-conditioned weighting unless machine identity/state is available before the draw;
- no production weight for Richardson;
- no retrospective tuning of `tau`, `h`, `kappa`, blend weights or stage placement from revealed outcomes.

## Promotion policy

One good target is diagnostic, not promotion evidence. E0018 remains a long-horizon prospective shadow governed by E0011 power/redundancy controls and the current method doctrine.

Current binding interpretation:

- `governance/current_method_doctrine.md`
- `governance/methodology_deprecations.md`
- `governance/research_protocol.md`
