# E0020 Decision

## Original decision

`RUN PROSPECTIVE SHADOW / DO NOT MODIFY PRODUCTION`

Evidence classification: `INSUFFICIENT_EVIDENCE`.

## 2026-09-02 post-target decision

### Retain

Terminal motifs remain a legitimate **diagnostic** research family when tested with:

- exact slot-specific terminal nulls;
- strict walk-forward suffix matching;
- multiplicity-aware sequence diagnostics;
- finite preregistered algebraic libraries;
- no XTRA-fitted-state transfer into Main.

### Reject for forward coordinate acquisition

The E0020 nested multiplicative chain:

- M1 = structural coordinate PMF × terminal residual ratio;
- M2 = M1 × HLR residual ratio;
- M3 = M2 × VVD residual ratio;

is **rejected for forward candidate acquisition**.

Reason:

- terminal, HLR and VVD are deterministic views of the same target slot transition;
- sequential multiplication compounds non-independent information;
- discovery replay coordinate log loss worsened monotonically M0 -> M1 -> M2 -> M3;
- the current-method doctrine now requires one coherent transition representation rather than independent products of derived views.

Historical M1-M3 artifacts remain immutable evidence of what was tested.

## 2026-09-01 prospective target

Actual Main: `14,16,31,34,40 | PB4`.

The frozen terminal shadow produced useful but mixed target-specific information:

- S3 terminal `1` was a top-1 hit;
- S4 terminal `4` was a top-1 hit;
- S1 algebraic residue `4` diagnostic was correct;
- S2 terminal `6` and S5 terminal `0` were present in secondary terminal support.

These target hits do not override the negative historical proper-score evidence and therefore do not justify global terminal authority.

## Forward authority

Terminal motifs may:

- report full terminal probability fields;
- contribute a bounded preservation hedge when they agree with stronger independent evidence;
- be tested as a separate calibrated probability model;
- be used as a basis function inside one coherent signed-transition model.

Terminal motifs may not:

- hard-eliminate coordinates;
- receive an independent convergence vote with HLR/VVD from the same transition;
- be multiplied with HLR and VVD ratios;
- change K by union expansion;
- receive promotion from one successful draw.

## Current status

- Historical E0020 shadow: immutable and scoreable.
- Terminal diagnostic family: retained.
- M1-M3 multiplicative acquisition chain: `REJECT` for forward reuse.
- Production authority: none.
- Evidence classification: `INSUFFICIENT_EVIDENCE`.