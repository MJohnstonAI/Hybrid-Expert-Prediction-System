# E0028 Decision — Last-Digit Sum Absolute-Delta Constraint

## Decision

`ACCEPT AS DISCOVERY-ONLY PROSPECTIVE SHADOW / NO HARD-PRUNING AUTHORITY`

Evidence classification: `INSUFFICIENT_EVIDENCE`.

Paper trading only.

## Main discovery result

Across 26 consecutive Main Mechanical-Era transitions through 2026-09-01, the band `LDSAD in 11..13` occurred 11 times (42.31%). The exact IID 5/50 structural-null probability for the same fixed band is approximately 10.94%, an observed/null lift of about 3.87x.

This concentration is notable enough to justify a prospective shadow constraint.

## Why this is not promoted

The band was discovered after inspecting historical transitions. The search therefore has multiplicity and neighboring-band exposure. The historical concentration is not confirmation and receives zero retrospective predictive credit.

## Allowed forward use

- compute LDSAD for every future Main draw;
- freeze `11..13` as the primary Main discovery band before the next outcome;
- also score the predeclared broader bands `10..13` and `9..13` as separate challengers;
- after HLR/E0026 constraints are applied, measure how many surviving legal lines each LDSAD band removes;
- score whether the actual winning line survives;
- compare with exact structural-null expected retention and matched random-band controls.

## Forbidden forward use

- no hard deletion from production slates yet;
- no changing the favored LDSAD band after seeing the result;
- no Main-to-XTRA fitted-band transfer;
- no claim that the last digits physically influence the draw;
- no independent-expert vote multiplication with other terminal-derived diagnostics without a redundancy audit.

## Main 2026-09-04 shadow mapping

The prior Main draw on 2026-09-01 has SLD=15. Under the frozen primary band `11..13`, the next SLD-sum shadow targets are:

`2,3,4,26,27,28`.

This is a diagnostic constraint only and has zero authority to rewrite a previously frozen slate unless a separately governed pre-draw challenger is frozen before outcome knowledge.

## XTRA decision

`METHOD TRANSFER ONLY`.

The XTRA session may compute the same LDSAD statistic from the independent post-2026-06-02 XTRA ledger, but it must derive, audit and freeze its own band. The Main 11..13 discovery band has no XTRA predictive authority.

## Promotion criterion

A future promotion requires multiple prospective targets showing that LDSAD produces material incremental compression after HLR/E0026 while preserving the winning line at a very high rate, with multiplicity-aware evidence and independent reproduction.