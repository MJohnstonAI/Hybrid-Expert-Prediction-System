# E0022 Decision

## Decision

`ACCEPT METHODOLOGY / ACCEPT OPTIONAL PORTFOLIO GEOMETRY / HOLD PREDICTIVE AUTHORITY`

Evidence classification: `INSUFFICIENT_EVIDENCE`.

Paper trading only.

## Accepted immediately

### 1. Tie-safe oracle ranking

Average midrank is the required E0022 ranking rule for tied coalition scores.

A discrete score may not give every tied line the best rank in its tie block and then cite the resulting percentile as predictive lift.

Historical artifacts remain immutable, but future interpretation of E0014 raw-pair oracle results must include the E0022 tie correction.

### 2. Optional `four_plus_first` Johnson objective

After a candidate universe and line budget are frozen, `JOHNSON_COVER_PORTFOLIO` may optionally prioritize new 4+/5 winner-state coverage before 3+ coverage.

This authority is deterministic portfolio geometry only.

It may not:

- score or alter candidates;
- increase K;
- increase line budget;
- claim 4+ coverage gain as predictive information;
- claim an exact 5/5 probability gain without an independently validated non-uniform winner-state model.

The historical `three_plus_first` objective remains available for backward compatibility and audit reproduction.

## Prospective shadow only

### `MAIN_ASSEMBLY_DISSENT_OR`

Frozen formula:

`max(midrank_percentile(frequency), midrank_percentile(recency), midrank_percentile(E0013_spectral))`

Purpose: test whether bounded alternative-lane support reduces catastrophic burial of the true line when E0013 is wrong.

Authority:

- coalition/winner-float shadow only;
- zero candidate authority;
- zero production weight;
- zero independent-vote interpretation;
- fixed K13 and fixed line budget only.

First eligible prospective target after E0022 freeze: `2026-09-04`.

Promotion requires repeated prospective reduction in catastrophic burial and/or improved exact-winner rank against E0013 and simple controls at identical exposure.

## Rejected / not promoted

- four-node nucleus current formulation;
- conditional-completion current formulation;
- simple PMI as a Main breakthrough;
- XTRA raw-pair coalition ranking as a breakthrough;
- direct Main E0013 spectral transfer to XTRA;
- any searched E0022 retrospective fusion as production authority.

## XTRA decision

No XTRA predictive coalition assembler is promoted.

The old strong-looking raw-pair oracle result is downgraded because average-midrank scoring removes the apparent advantage. XTRA may still use Johnson geometry after its own frozen K13, but that geometry has no predictive authority.

## Search/multiplicity warning

E0022 screened a large family of rankers and fusions after the outcomes already existed. All predictive results are discovery/post-hoc evidence. No `BREAKTHROUGH` claim is permitted from this package.

## Forward architecture

### Main

`frozen K13 -> enumerate all 1,287 lines -> E0013 incumbent shadow + MAIN_ASSEMBLY_DISSENT_OR challenger -> fixed-budget portfolio -> optional four_plus_first Johnson geometry`

### XTRA

`frozen K13 -> enumerate all 1,287 lines -> no promoted predictive coalition ranker -> optional four_plus_first Johnson geometry`

The exact winner line must never be removed from a correct K13 by morphology or generation shortcuts before ranking.
