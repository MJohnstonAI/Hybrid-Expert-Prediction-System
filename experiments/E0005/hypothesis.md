# E0005 — Structural-Residual Direction and VVD Championship

## Originating session

ChatGPT post-2026-08-14 synthesis incorporating Grok BARP feedback and the E0003 first prospective result.

## Self-selected role

State-space modeller / adversarial statistician.

## Target stages

`slot_forecast | candidate_funnel`

## Hypothesis A — BARP

A duration-aware HLR model can improve proper probability score beyond `NULL_HLR_STRUCTURAL` and plain empirical HLR by conditioning persistence-versus-flip odds on the current directional run length.

Proposed name: Boundary-Adjusted Run Persistence (BARP).

## Hypothesis B — VVD-R

A heavily shrunk distributional residual model over legal VVD magnitudes can improve proper probability score beyond `NULL_VVD_STRUCTURAL` and simple empirical displacement frequency.

Proposed name: VVD-R (Vertical Variance Delta Residual Distribution).

## Why this matters

The 2026-08-14 structural modal HLR vector matched all five actual directions while E0003 exact VVD point forecasts missed Slots1/3/5. The correct research response is to separate direction from magnitude and evaluate both probabilistically against exact structural geometry.

## Anti-hindsight constraint

The post-draw observation that E0003 forecasts for Slots1/3/5 were each two VVD points below actual values receives zero retrospective credit. It may be logged as a future challenger only if frozen before a target.

## Relationship to existing experts

- `MAIN_HLR_SLOT`: BARP is a challenger/residual model, not a replacement.
- `MAIN_VVD_DELTA`: VVD-R is a probabilistic successor/challenger, not a redefinition of frozen E0003 rules.
- `NULL_HLR_STRUCTURAL` and `NULL_VVD_STRUCTURAL`: mandatory baselines, not ensemble votes.

## Evidence classification

`INSUFFICIENT_EVIDENCE`
