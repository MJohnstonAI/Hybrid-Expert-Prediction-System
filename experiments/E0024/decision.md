# E0024 Decision

## Decision

`ACCEPT NEGATIVE EVIDENCE / RETAIN E0022 PORTFOLIO / HOLD ALL NEW PREDICTIVE AUTHORITY`

Evidence classification: `INSUFFICIENT_EVIDENCE`.

Paper trading only.

## 1. Pairwise-overlap strategy

`REJECT AS REPLACEMENT FOR E0022 FOUR_PLUS_FIRST`.

The external balanced-overlap principle materially improves exposure balance but reduces the exact 4+/5 winner-state coverage that HEPS currently prioritizes:

- E0022: `788/1287`;
- E0024 balanced nibble: `772/1287`.

Balance may be used only as a secondary tie-break among portfolios with identical primary 4+/5 coverage. It receives no predictive authority.

## 2. Main machine conditioning

`RESEARCH LEAD ONLY / NO DEPLOYABLE AUTHORITY`.

The broad machine association test is non-significant (`p≈0.181`) and fixed tau=8 worsens Brier score. A heavily shrunk tau=64 retrospective partition produced a tiny Brier improvement and K13 coordinate capture `3 -> 6` over five later targets.

This is insufficient for promotion because:

1. E0024 was designed after those outcomes existed;
2. the proper-score improvement is tiny;
3. the sample is five targets;
4. the exact target machine is not demonstrated to be knowable before ticket cutoff;
5. the NLC states that machines and ball sets are randomly selected.

Do not revise a Main slate using machine-specific numbers unless a future protocol satisfies the deployability gate and then earns prospective proper-score lift.

## 3. XTRA machine conditioning

`REJECT CURRENT FORM`.

No machine association (`p≈0.529`), fixed conditioning worsens Brier, and the heavy-shrinkage retrospective partition worsens Brier and reduces K13 capture `6 -> 5`.

Do not transfer the weak Main machine lead into XTRA.

## 4. Change-point gate

`NO PROMOTION`.

Main max-scan `p≈0.132`; XTRA `p≈0.488`. No state reset, CUSUM authority, or outcome-defined regime boundary is permitted from these results.

## 5. Physical video/dynamics

`DATA GAP / NOT TESTABLE`.

No predictive physics authority may be claimed until HEPS has a reproducible archive of actual pre-outcome physical-motion features. Graphical result animations are not admissible substitutes.

## 6. Friday 2026-09-04

**No Main or XTRA prediction slate should be revised because of E0024.**

The only immediately binding portfolio rule remains E0022 `four_plus_first` Johnson geometry. Main machine conditioning remains an external-data research question for a future prospectively deployable experiment.

## Next external-data research priority

Acquire reliable post-June `machine + ball_set` provenance, with timestamps and source URLs. If the exact machine/ball-set selection is not knowable pre-cutoff, test a prospectively knowable mixture over the declared active equipment pool instead of using oracle machine identity.
